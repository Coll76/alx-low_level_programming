#include "main.h"
#include <stdio.h>
/**
 * mul - 'Multiplication of two integers'
 * @a: first integer
 * @b: second integer
 * Return: Always 0
 */
int mul(int a, int b)
{
	int total;

	total = a * b;
return (total);
}
