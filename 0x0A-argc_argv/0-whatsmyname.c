#include <stdio.h>
/**
 * main - name of programm
 * @argc: arguement count
 * @argv: arguement vector
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	if (argc != 0)
	{
	printf("%s\n", argv[0]);
	}
	return (0);
}
