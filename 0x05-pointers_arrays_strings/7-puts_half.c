#include "main.h"
/**
 * puts_half - 'prints half of a string'
 * @str: string
 */
void puts_half(char *str)
{
	int strlen(char *s)
	{
	int longi = 0;

	while (*s != '\0')
	{
	longi++;
	s++;
	}
	return (longi);
}
